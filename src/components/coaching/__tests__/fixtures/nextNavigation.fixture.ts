export function useRouter() { return { refresh: () => { (window as Window & { __PROGRAM_REFRESHES__?: number }).__PROGRAM_REFRESHES__ = ((window as Window & { __PROGRAM_REFRESHES__?: number }).__PROGRAM_REFRESHES__ ?? 0) + 1 }, push: () => {} } }
